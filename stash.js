// For user to see a list of quotes they've learnt, 
// use the following code to arrange them into a list

console.log([1, 2].toString());
// → 1,2

let toBeListed = deletedFromLIstOnceLearnt.splice(tocreateanewarray);

toBeListed.toString()

// => quote 1, quote 2, etc;